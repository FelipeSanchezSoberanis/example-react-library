import { createTheme } from "@mui/material";

export const styleOne = createTheme({
  palette: {
    primary: {
      main: "#a161b8"
    }
  }
});

export const styleTwo = createTheme({
  palette: {
    primary: {
      main: "#d99e42"
    }
  }
});

export const styleThree = createTheme({
  palette: {
    primary: {
      main: "#370610"
    }
  }
});
